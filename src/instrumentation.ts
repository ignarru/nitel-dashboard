/**
 * Hook que Next.js corre al boot. Vacío por ahora — el envío es directo desde
 * el server action `enviarAhora`, no hay scheduler in-process.
 */
export async function register() {
  // intencionalmente vacío
}
